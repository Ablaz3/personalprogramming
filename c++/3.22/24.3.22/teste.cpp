#include <iostream>
#include <thread>
#include <chrono>
#include <sstream>
using namespace std;


int main ()
{
    int a, b, c, d;
    for (a=10; a>0; --a)
    {
        cout << a << '\n';
    }
    cout << "liftoff";
}